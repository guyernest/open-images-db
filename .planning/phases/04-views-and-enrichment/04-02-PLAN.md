---
phase: 04-views-and-enrichment
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - MASK-01

must_haves:
  truths:
    - "All 4 views exist in Athena and return rows"
    - "labeled_images view has display_name column with human-readable label names"
    - "labeled_boxes view has computed box_area, box_width, box_height, aspect_ratio columns"
    - "labeled_masks view has computed box_area, click_count columns with non-null values"
    - "labeled_relationships view has display_name_1 and display_name_2 from double class_descriptions join"
    - "View row counts match or closely match base table row counts (no unexpected row loss from JOINs)"
  artifacts: []
  key_links:
    - from: "scripts/create-views.sh"
      to: "Athena open_images database"
      via: "athena_execute_and_wait executing CREATE OR REPLACE VIEW"
      pattern: "athena_execute_and_wait"
    - from: "scripts/verify-views.sh"
      to: "Athena open_images database"
      via: "athena_query_scalar checking view existence and columns"
      pattern: "athena_query_scalar"
---

<objective>
Execute the view creation pipeline against Athena and verify all views are queryable with correct data.

Purpose: Validate that the SQL views created in Plan 01 work correctly against the real Iceberg tables in AWS. This is the final step of Phase 4 -- after this, downstream consumers can query fully-denormalized views.

Output: 4 working Athena views verified by verify-views.sh
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-views-and-enrichment/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute create-views.sh to create all 4 views in Athena</name>
  <files></files>
  <action>
Run `bash scripts/create-views.sh` to create all 4 views in Athena. This will:
1. Source common.sh and athena.sh
2. Find all 4 SQL files in queries/views/
3. Substitute __DATABASE__ with open_images
4. Execute each CREATE OR REPLACE VIEW statement via Athena

If any view creation fails, inspect the error from Athena (logged by athena_execute_and_wait). Common issues:
- Column name mismatch: check actual table schemas vs SQL references
- Missing table: verify Phase 3 tables exist
- Syntax error: check Trino/Athena v3 SQL compatibility

Fix any SQL file issues found during execution, then re-run. CREATE OR REPLACE VIEW is idempotent so re-running is safe.
  </action>
  <verify>
    <automated>bash scripts/create-views.sh 2>&1 | tail -5</automated>
  </verify>
  <done>create-views.sh completes with 0 failures, all 4 views created in Athena</done>
</task>

<task type="auto">
  <name>Task 2: Run verify-views.sh to validate all views</name>
  <files></files>
  <action>
Run `bash scripts/verify-views.sh` (full mode, not --quick) to validate:
1. All 4 views exist and return rows
2. Computed columns (box_area, click_count, display_name_1, etc.) have non-null values
3. Row counts compared to base tables (warn if JOIN dropped rows)

If any verification fails:
- View doesn't exist: re-run create-views.sh
- Computed column is all NULL: check SQL formula references correct column names
- Row count significantly less than base table: investigate whether some label_name values are missing from class_descriptions. If so, document the gap but do not change the JOIN type (user decision is full denormalization with INNER JOIN)

All checks should pass. If row count warnings appear, document in the summary.
  </action>
  <verify>
    <automated>bash scripts/verify-views.sh 2>&1 | tail -10</automated>
  </verify>
  <done>verify-views.sh completes with all checks passed, confirming 4 views exist with correct data, computed columns are non-null, and row counts match expectations</done>
</task>

</tasks>

<verification>
- create-views.sh exits 0 with all 4 views created
- verify-views.sh exits 0 with all checks passed
- Manual spot check: `SELECT display_name, COUNT(*) FROM open_images.labeled_images GROUP BY display_name ORDER BY 2 DESC LIMIT 5` returns reasonable human-readable label names
</verification>

<success_criteria>
- All 4 Athena views are live and queryable
- Computed geometry columns produce non-null numeric values
- Click count works via split/cardinality on semicolon-delimited data
- Row counts validate JOIN correctness
- Phase 4 is complete -- users can query pre-joined views with human-readable labels
</success_criteria>

<output>
After completion, create `.planning/phases/04-views-and-enrichment/04-02-SUMMARY.md`
</output>
