---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - infra/lib/open-images-stack.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "cdk deploy completes successfully and all resources exist in AWS"
    - "S3 bucket exists with name open-images-{account}-{region}"
    - "Glue database open_images exists in the Glue catalog"
    - "Athena workgroup open-images exists with 10GB scan limit enforced"
    - "cdk destroy removes all resources including bucket contents"
  artifacts:
    - path: "infra/lib/open-images-stack.ts"
      provides: "Deployed CDK stack (no file changes expected)"
  key_links:
    - from: "cdk deploy"
      to: "AWS CloudFormation"
      via: "Stack creation"
      pattern: "OpenImagesStack"
---

<objective>
Deploy the CDK stack to AWS and verify all infrastructure resources exist and can be torn down cleanly.

Purpose: Confirm that the synthesized CloudFormation template actually provisions working resources in AWS — unit tests verify the template, this verifies reality.
Output: Live AWS resources ready to receive data in Phase 2, or issues identified for fixing.
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy CDK stack to AWS</name>
  <files>infra/lib/open-images-stack.ts</files>
  <action>
    Run `cd infra && npx cdk deploy --require-approval never` to deploy the stack.

    If deployment fails:
    - Read the CloudFormation error from the output
    - Fix the issue in `infra/lib/open-images-stack.ts`
    - Re-run `npx jest` to confirm tests still pass
    - Re-deploy

    After successful deployment, capture the stack outputs (bucket name, database name, workgroup name, policy ARN) from the deploy output.
  </action>
  <verify>
    <automated>cd /Users/guy/projects/open-images/infra && npx cdk deploy --require-approval never 2>&1 | tail -30</automated>
  </verify>
  <done>CDK stack deployed. CloudFormation outputs show bucket name, database name, workgroup name, and policy ARN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployed infrastructure in AWS Console</name>
  <action>
    User verifies all deployed resources match expectations. No automated action — this is a human verification checkpoint.
  </action>
  <what-built>Complete AWS infrastructure stack: S3 bucket (with raw/, warehouse/, athena-results/ prefix zones), Glue database (open_images), Athena workgroup (open-images with 10GB scan limit), and scoped IAM policy.</what-built>
  <how-to-verify>
    1. Open AWS Console > S3 — verify bucket `open-images-{account}-{region}` exists
    2. Open AWS Console > Glue > Databases — verify `open_images` database exists with location pointing to `s3://open-images-{account}-{region}/warehouse/`
    3. Open AWS Console > Athena > Workgroups — verify `open-images` workgroup exists, click it to confirm:
       - Query data usage control: 10 GB per query
       - Engine version: Athena engine version 3
       - Output location: `s3://open-images-{account}-{region}/athena-results/`
    4. Open AWS Console > Athena > Query editor — select `open-images` workgroup and `open_images` database, run: `SELECT 1 AS test` — should succeed
    5. (Optional) Test teardown: run `cd infra && npx cdk destroy --force` then verify resources are gone, then re-deploy with `npx cdk deploy --require-approval never`
  </how-to-verify>
  <verify>User confirms all resources exist and Athena can execute queries</verify>
  <done>User approved deployment. All AWS resources verified.</done>
  <resume-signal>Type "approved" if all resources verified, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cdk deploy` completed without errors
2. Stack outputs match expected naming convention
3. User confirmed resources exist in AWS Console
4. Athena can execute a simple query against the workgroup
</verification>

<success_criteria>
- CDK stack deployed successfully to AWS
- All 5 resource types confirmed to exist (S3, Glue DB, Athena workgroup, IAM policy, auto-delete Lambda)
- User approved the deployment
- Infrastructure is ready for Phase 2 (data acquisition)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>
