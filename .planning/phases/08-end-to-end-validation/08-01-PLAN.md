---
phase: 08-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - queries/examples/00-mcp-reference.sql
  - queries/examples/05-entity-search.sql
  - queries/examples/06-category-exploration.sql
  - queries/examples/07-image-contents.sql
  - queries/examples/08-relationship-inventory.sql
autonomous: true
requirements:
  - AUDIT-04

must_haves:
  truths:
    - "MCP reference resource contains schema, common values, query patterns, and pitfalls in a single LLM-injectable SQL file"
    - "Four new example queries cover entity search, category exploration, image contents, and relationship inventory use cases"
    - "Each example query includes natural language prompt header, SQL, and expected output comments"
  artifacts:
    - path: "queries/examples/00-mcp-reference.sql"
      provides: "LLM-optimized reference for MCP code mode context injection"
      contains: "Schema with semantics, common values, query patterns, known pitfalls"
    - path: "queries/examples/05-entity-search.sql"
      provides: "Entity search example using hierarchy expansion + relationship filter"
    - path: "queries/examples/06-category-exploration.sql"
      provides: "Category exploration example using class_hierarchy subtree"
    - path: "queries/examples/07-image-contents.sql"
      provides: "Image contents example joining labels, boxes, relationships for single image"
    - path: "queries/examples/08-relationship-inventory.sql"
      provides: "Relationship inventory example using hierarchy_relationships grouped by type"
  key_links:
    - from: "queries/examples/00-mcp-reference.sql"
      to: "All views and tables"
      via: "Schema documentation references"
      pattern: "class_hierarchy|hierarchy_relationships|labeled_"
    - from: "queries/examples/05-08*.sql"
      to: "queries/views/*.sql"
      via: "SQL queries against views"
      pattern: "__DATABASE__\\."
---

<objective>
Create the MCP reference resource and four new example queries that demonstrate real-world MCP server usage patterns for the Open Images dataset.

Purpose: Provide the MCP client LLM with a dense, injectable reference for generating SQL on-the-fly (code mode), and supply pre-built example queries covering the primary use cases (tool mode). These are the artifacts that prove the data is discoverable and useful.

Output: 5 SQL files in queries/examples/ (00, 05, 06, 07, 08)
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@reports/06-audit-report.md
@queries/views/05-class-hierarchy.sql
@queries/views/06-hierarchy-relationships.sql
@queries/views/04-labeled-relationships.sql
@queries/views/01-labeled-images.sql
@queries/examples/01-people-on-horses.sql
@queries/examples/02-hierarchy-browsing.sql
@queries/examples/03-relationship-discovery.sql
@queries/examples/04-subtree-statistics.sql

<interfaces>
<!-- Tables (base Iceberg tables): -->
<!-- images: image_id, original_url, original_landing_url, license, author, title, original_size, thumbnail_300k_url -->
<!-- labels: image_id, label_name, source, confidence -->
<!-- boxes: image_id, label_name, x_min, x_max, y_min, y_max, is_occluded, is_truncated, is_group_of, is_depiction, is_inside, clicks -->
<!-- masks: image_id, label_name, mask_path, predicted_iou, clicks -->
<!-- relationships: image_id, label_name_1, label_name_2, relationship_label, x_min_1..y_max_2 (8 bbox cols) -->
<!-- class_descriptions: label_name (MID like /m/01g317), display_name (human-readable like Person) -->
<!-- label_hierarchy: parent_mid, child_mid, edge_type -->

<!-- Views: -->
<!-- labeled_images: labels + images + class_descriptions (display_name, source, confidence) -->
<!-- labeled_boxes: boxes + images + class_descriptions -->
<!-- labeled_masks: masks + images + class_descriptions -->
<!-- labeled_relationships: relationships + images + class_descriptions (display_name_1, display_name_2) -->
<!-- class_hierarchy: recursive CTE with mid, display_name, parent_mid, parent_name, depth, edge_type, root_path, is_leaf -->
<!-- hierarchy_relationships: labeled_relationships expanded through class hierarchy with ancestor_name_1, ancestor_name_2, depth_1, depth_2 -->

<!-- Key data from audit report: -->
<!-- 27 relationship types, 27,243 total relationship rows, 26,357 in view (886 dropped by INNER JOIN) -->
<!-- 602 MIDs in hierarchy, 5 depth levels, single root "Entity" -->
<!-- Top relationships: is (22,292), wears (1,491), at (662), holds (566), contain (540) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MCP reference resource</name>
  <files>queries/examples/00-mcp-reference.sql</files>
  <action>
Create queries/examples/00-mcp-reference.sql as a dense, LLM-optimized SQL comment file designed for MCP code mode context injection. Use SQL comment blocks (-- prefixed) throughout. The file should be self-contained — an LLM reading only this file can write correct queries.

Four sections:

**Section 1: Schema with semantics**
- Document all 7 base tables (images, labels, boxes, masks, relationships, class_descriptions, label_hierarchy) with column names, types, and plain-English descriptions
- Document all 6 views (labeled_images, labeled_boxes, labeled_masks, labeled_relationships, class_hierarchy, hierarchy_relationships) with column names and what they join/compute
- Use `__DATABASE__` placeholder consistently (mention it must be replaced with `open_images`)

**Section 2: Common values enumeration**
- All 27 relationship types with counts (from audit report data: is=22292, wears=1491, at=662, holds=566, contain=540, on=436, ride=361, hang=167, plays=158, interacts_with=121, dance=109, inside_of=81, kiss=77, hug=70, skateboard=42, surf=17, throw=9, read=9, kick=8, drink=8, hits=7, catch=4, under=4, eat=1, cut=1, ski=1, snowboard=1)
- Top hierarchy branches: Entity root, major children (Person->Man/Woman/Boy/Girl, Animal->Dog/Cat/Horse/..., Food, Clothing, Vehicle, Furniture, etc.) sourced from audit report
- Note: MIDs are internal identifiers like /m/01g317 — always use display_name for human-readable queries

**Section 3: Query pattern cookbook**
Include reusable SQL patterns as executable examples with comments:
1. Hierarchy expansion pattern: query Person to find Man/Woman/Boy/Girl via ancestor_name columns
2. Subtree listing: recursive class_hierarchy with depth/root_path
3. Multi-table join: image contents combining labels + boxes + relationships
4. Relationship filtering by ancestor class
5. Aggregation with window functions: rank relationship types by count within ancestor groups
6. CTE for complex analysis: combining hierarchy + relationships

**Section 4: Known pitfalls**
- Raw MIDs vs display names: label_name columns contain MIDs (/m/...), use display_name columns or join class_descriptions
- INNER JOIN drops: labeled_relationships drops ~3.3% of rows (886 of 27,243) due to 3 orphan MIDs missing from class_descriptions
- edge_type filtering: label_hierarchy has edge_type column (subcategory, part) — filter if needed
- Recursive CTE depth: views cap at 20 levels (only 5 exist in practice)
- Relationship table uses leaf classes, not parent classes — use hierarchy_relationships view for parent-class queries
- The `is` relationship type dominates (81.8%) — filter it out for action/spatial relationship analysis
  </action>
  <verify>
    <automated>test -f queries/examples/00-mcp-reference.sql && grep -c "Section" queries/examples/00-mcp-reference.sql | grep -q "[4-9]" && grep -q "hierarchy_relationships" queries/examples/00-mcp-reference.sql && grep -q "pitfall" queries/examples/00-mcp-reference.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>00-mcp-reference.sql exists with all 4 sections (schema, common values, patterns, pitfalls), references all tables/views, includes all 27 relationship types, and contains executable SQL pattern examples</done>
</task>

<task type="auto">
  <name>Task 2: Create four new MCP-pattern example queries</name>
  <files>queries/examples/05-entity-search.sql, queries/examples/06-category-exploration.sql, queries/examples/07-image-contents.sql, queries/examples/08-relationship-inventory.sql</files>
  <action>
Create 4 new example query files following the exact pattern of existing examples 01-04 (comment header with natural language prompt, SQL with __DATABASE__ placeholder, expected output comments).

**05-entity-search.sql** — "Find images of dogs playing"
- Comment header: natural language MCP prompt "find images of dogs playing"
- Query 1: Use hierarchy_relationships to find all play-related relationships (plays, interacts_with) involving Dog and its ancestors via ancestor_name columns. Select image_id, display_name_1, display_name_2, relationship_label, thumbnail_300k_url. LIMIT 20.
- Query 2: Expand to all Animal subclasses playing — group by display_name_1, relationship_label with counts
- Include expected output comments based on audit data (Dog is under Animal in hierarchy)

**06-category-exploration.sql** — "What categories exist under Animal?"
- Comment header: "What categories exist under Animal?"
- Query 1: Use class_hierarchy WHERE parent_name = 'Animal' to list direct children with depth, edge_type, is_leaf
- Query 2: Full subtree under Animal — WHERE root_path LIKE 'Entity > Animal%' with depth and root_path, ordered by depth then name
- Include expected output comments (from audit: Animal has children like Dog, Cat, Horse, Bird, Fish, etc.)

**07-image-contents.sql** — "What's in this image?"
- Comment header: "What's in this image? (given an image_id)"
- Use a sample image_id from relationships (pick one known to have relationships — comment that any image_id can be substituted)
- Query 1: All labels for the image from labeled_images (SELECT display_name, source, confidence)
- Query 2: All bounding boxes from labeled_boxes (SELECT display_name, x_min, x_max, y_min, y_max)
- Query 3: All relationships from labeled_relationships (SELECT display_name_1, relationship_label, display_name_2)
- Comment: in practice, an MCP tool would combine these or use UNION ALL

**08-relationship-inventory.sql** — "What relationships involve cars?"
- Comment header: "What relationships involve cars?"
- Query 1: Use hierarchy_relationships WHERE ancestor_name_1 = 'Car' OR ancestor_name_2 = 'Car', group by relationship_label, count instances
- Query 2: Specific entity pairs — group by display_name_1, display_name_2, relationship_label where Car is ancestor, order by count DESC, LIMIT 20
- Include expected output comments (Car is in hierarchy under Vehicle)
  </action>
  <verify>
    <automated>for f in 05-entity-search 06-category-exploration 07-image-contents 08-relationship-inventory; do test -f "queries/examples/${f}.sql" && grep -q "__DATABASE__" "queries/examples/${f}.sql" && grep -q "SELECT" "queries/examples/${f}.sql" || { echo "FAIL: $f"; exit 1; }; done && echo "PASS"</automated>
  </verify>
  <done>Four example query files exist (05-08), each with natural language prompt header, SQL using __DATABASE__ placeholder, and expected output comments. Queries cover entity search, category exploration, image contents, and relationship inventory patterns.</done>
</task>

</tasks>

<verification>
- All 5 new SQL files exist in queries/examples/
- Every SQL file uses __DATABASE__ placeholder (not hardcoded database name)
- MCP reference covers all 4 sections with accurate data from audit report
- Example queries follow same comment/SQL pattern as existing 01-04 files
- No existing files modified
</verification>

<success_criteria>
- queries/examples/00-mcp-reference.sql is a comprehensive, LLM-injectable reference containing schema, common values, patterns, and pitfalls
- queries/examples/05-08*.sql each demonstrate a distinct MCP usage pattern with executable SQL
- All files use __DATABASE__ placeholder consistently
</success_criteria>

<output>
After completion, create `.planning/phases/08-end-to-end-validation/08-01-SUMMARY.md`
</output>
