---
phase: 05-validation-and-query-surface
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false
requirements: [VAL-01, VAL-02, QUERY-01, QUERY-02, QUERY-03]

must_haves:
  truths:
    - "All 7 Iceberg table row counts match raw external table counts within 1% tolerance"
    - "Spot-check queries confirm valid value ranges and non-null columns"
    - "All example SQL queries execute successfully against live Athena"
  artifacts: []
  key_links:
    - from: "scripts/validate-data.sh"
      to: "Athena open_images database"
      via: "athena_query_scalar calls"
      pattern: "athena_query_scalar"
---

<objective>
Execute validation script against live Athena and verify all example queries work.

Purpose: Confirm data quality and ensure the query surface is accurate (no typos, no schema mismatches).
Output: Validation results (all pass), verified example queries
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-and-query-surface/05-01-SUMMARY.md

@scripts/validate-data.sh
@scripts/lib/common.sh
@scripts/lib/athena.sh
@docs/examples.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run validate-data.sh against live Athena</name>
  <files></files>
  <action>
Execute the validation script against the live Athena database:

```bash
bash scripts/validate-data.sh
```

This runs both Phase 1 (row count validation for all 7 tables) and Phase 2 (spot-check validation).

If any checks fail:
- For row count mismatches exceeding 1%: investigate the specific table, compare raw vs Iceberg schemas
- For spot-check failures: check if the CASE WHEN validation query is correct, fix if needed
- Fix any issues in validate-data.sh and re-run

Expected outcome: All checks pass (possibly with WARN for minor count differences within tolerance).
  </action>
  <verify>bash scripts/validate-data.sh 2>&1 | tail -10</verify>
  <done>validate-data.sh completes with all checks passing (0 failures), row counts match within tolerance, spot-checks confirm valid value ranges</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final project verification checkpoint</name>
  <files></files>
  <action>Present validation results and documentation artifacts for human review.</action>
  <what-built>
Data validation script executed against live Athena, confirming:
- Row counts for all 7 Iceberg tables match their raw external table counterparts
- Spot-check queries verified non-null columns and valid value ranges
- All documentation artifacts (SCHEMA.md, examples.md) are complete

This is the final verification for the entire project (Phase 5 of 5).
  </what-built>
  <how-to-verify>
1. Review validation output from Task 1 (shown above)
2. Optionally run a quick validation yourself: `bash scripts/validate-data.sh --quick`
3. Optionally pick 1-2 queries from `docs/examples.md` and run them in the Athena console
4. Browse `docs/SCHEMA.md` to confirm it covers the tables/views you care about
5. Confirm the query surface is sufficient for the downstream MCP server team
  </how-to-verify>
  <verify>Human approval received</verify>
  <done>Human confirms validation results and documentation quality are acceptable</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `bash scripts/validate-data.sh` exits 0 with all checks passing
- Example queries from docs/examples.md are syntactically valid SQL
</verification>

<success_criteria>
- Validation script runs to completion with 0 failures against live Athena
- Human confirms the validation results and documentation quality
- Phase 5 (and the entire v1 milestone) is complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-query-surface/05-02-SUMMARY.md`
</output>
