---
phase: 02-data-acquisition
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06]

must_haves:
  truths:
    - "All 5 annotation CSVs exist in S3 raw/annotations/ with non-zero sizes"
    - "All 3 metadata files exist in S3 raw/metadata/ with non-zero sizes"
    - "Mask PNGs exist in S3 raw/masks/ with count ~24,730"
    - "raw/manifest.json exists in S3 with complete file listing"
    - "Running download-all.sh a second time completes quickly without re-uploading"
  artifacts:
    - path: "s3://$BUCKET/raw/annotations/"
      provides: "5 annotation CSV files"
    - path: "s3://$BUCKET/raw/metadata/"
      provides: "3 metadata CSV files"
    - path: "s3://$BUCKET/raw/masks/"
      provides: "~24,730 segmentation mask PNGs"
    - path: "s3://$BUCKET/raw/manifest.json"
      provides: "JSON manifest of all downloaded files"
  key_links:
    - from: "scripts/download-all.sh"
      to: "s3://$BUCKET/raw/"
      via: "aws s3 sync uploads"
      pattern: "aws s3 sync"
---

<objective>
Run the data acquisition pipeline and verify all Open Images V7 validation data lands correctly in the S3 raw zone.

Purpose: Confirm the scripts work end-to-end and data is ready for Phase 3 (Iceberg table creation).
Output: Populated S3 raw zone with annotations, metadata, masks, and manifest.
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-acquisition/02-CONTEXT.md
@.planning/phases/02-data-acquisition/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run pipeline and verify S3 data</name>
  <files>scripts/download-all.sh</files>
  <action>
Human runs the data acquisition pipeline and verifies S3 contents.

**What was built:** Complete data acquisition pipeline (scripts/download-all.sh) that downloads all Open Images V7 validation annotations, metadata, and segmentation masks from public HTTPS URLs to S3.

**How to verify:**
1. Ensure AWS CDK stack is deployed first: `cd infra && npx cdk deploy --profile ze-kasher-dev`
2. Run the pipeline: `bash scripts/download-all.sh`
   - On EC2: Run directly (recommended for masks due to bandwidth)
   - Locally: Works but mask download may take longer
   - Quick test without masks: `bash scripts/download-all.sh --skip-masks`
3. Verify the script output shows:
   - Prerequisites check passes
   - Bucket name discovered from CloudFormation
   - 5 annotation CSVs downloaded and uploaded
   - 3 metadata files downloaded and uploaded
   - 16 mask archives downloaded, extracted, and uploaded (~24,730 PNGs)
   - Post-download validation passes (correct file counts)
   - Manifest generated and uploaded
4. Verify S3 contents manually:
   ```
   aws s3 ls s3://BUCKET/raw/ --profile ze-kasher-dev --recursive --summarize
   ```
5. Test idempotency: Run `bash scripts/download-all.sh` again -- should complete quickly with minimal re-work (DATA-04)
6. Verify manifest: Download and inspect raw/manifest.json has entries for all files

**Resume signal:** Type "approved" if all data is in S3 with correct counts, or describe any issues encountered.
  </action>
  <verify>
    <automated>bash scripts/download-all.sh --validate-only</automated>
  </verify>
  <done>All annotation CSVs, metadata, and mask PNGs confirmed in S3 raw zone with expected file counts. Manifest exists. Idempotent re-run confirmed.</done>
</task>

</tasks>

<verification>
S3 raw zone populated: raw/annotations/ has 5 CSVs, raw/metadata/ has 3 CSVs, raw/masks/ has ~24,730 PNGs, raw/manifest.json exists. Second run of download-all.sh is idempotent.
</verification>

<success_criteria>
- All annotation CSVs, metadata files, and mask PNGs are in S3 raw zone
- File counts match expected values from research (5 annotations, 3 metadata, ~24,730 masks)
- Manifest.json exists with complete listing
- Pipeline is idempotent (second run completes without corruption)
- User confirms data is correct
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-acquisition/02-02-SUMMARY.md`
</output>
