---
phase: 03-iceberg-tables
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: false
requirements: [TBL-01, TBL-02, TBL-03, TBL-04, TBL-05, TBL-06, TBL-07, TBL-08, TBL-09, TBL-10]

must_haves:
  truths:
    - "All 7 Iceberg tables exist and return rows when queried"
    - "Column types are correct: DOUBLE for coordinates/confidence, BOOLEAN for flags, INT for original_size"
    - "Labels table contains data from both human and machine label sources"
    - "Label hierarchy table contains parent-child edges"
    - "Tables are Iceberg format backed by Parquet in warehouse/ zone"
  artifacts: []
  key_links:
    - from: "scripts/create-tables.sh"
      to: "queries/tables/*.sql"
      via: "reads and executes SQL files"
      pattern: "queries/tables/"
    - from: "scripts/verify-tables.sh"
      to: "open_images.*"
      via: "SELECT queries against Iceberg tables"
      pattern: "FROM open_images"
---

<objective>
Execute the table creation pipeline and verify all 7 Iceberg tables are queryable with correct schemas and types.

Purpose: This is the end-to-end validation that transforms raw CSVs into production Iceberg tables. Human verification ensures the pipeline ran successfully against real AWS infrastructure.

Output: 7 populated Iceberg tables in the open_images Glue database, verified by automated checks and human confirmation.
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-iceberg-tables/03-CONTEXT.md
@.planning/phases/03-iceberg-tables/03-01-SUMMARY.md
@.planning/phases/03-iceberg-tables/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute create-tables.sh and verify all Iceberg tables</name>
  <files></files>
  <action>
Human-verify checkpoint. Claude should NOT execute the pipeline (it requires AWS credentials and costs money). Instead, present the verification steps below and wait for human approval.

What was built:
- scripts/create-tables.sh reorganizes raw CSVs to per-table S3 prefixes, downloads and flattens hierarchy JSON, then executes 7 SQL files creating 14 external tables and 7 Iceberg tables
- scripts/verify-tables.sh checks all 7 tables have rows and correct column types
- 7 SQL files in queries/tables/ with DROP + CREATE EXTERNAL + CTAS patterns
  </action>
  <verify>
Human runs these commands and reports results:

1. Run the table creation pipeline:
   ```bash
   bash scripts/create-tables.sh
   ```
   Expected: All 7 tables created successfully. Runner reports success for each SQL file.
   This may take 2-5 minutes as Athena processes each CTAS query.

2. Run the verification script:
   ```bash
   bash scripts/verify-tables.sh
   ```
   Expected: All 7 tables have row count > 0. Type checks pass (DOUBLE for coordinates, BOOLEAN for flags).

3. Quick spot-check in Athena console (optional):
   - Visit https://console.aws.amazon.com/athena
   - Select workgroup: open-images
   - Run: `SELECT * FROM open_images.images LIMIT 5`
   - Run: `SELECT * FROM open_images.bounding_boxes LIMIT 5`
   - Verify columns look correct (snake_case names, numeric types, boolean values)

4. If any table creation failed:
   - Check the runner output for the Athena error message
   - Common issues: CSV header mismatch, wrong column count, S3 path not found
   - Fix the SQL file and re-run (pipeline is idempotent)

Resume signal: Type "approved" if all 7 tables created and verified, or describe any issues.
  </verify>
  <done>All 7 Iceberg tables (images, class_descriptions, labels, bounding_boxes, masks, relationships, label_hierarchy) exist in open_images database, return rows, and have correct column types. verify-tables.sh passes all checks.</done>
</task>

</tasks>

<verification>
After human approval:
- All 7 Iceberg tables exist in open_images database
- verify-tables.sh passes with all checks green
- Tables are backed by Parquet in s3://BUCKET/warehouse/
</verification>

<success_criteria>
- create-tables.sh completes without errors
- verify-tables.sh reports all 7 tables have rows > 0
- Type checks pass: DOUBLE coordinates, BOOLEAN flags, DOUBLE confidence
- Human confirms tables are queryable in Athena console
</success_criteria>

<output>
After completion, create `.planning/phases/03-iceberg-tables/03-03-SUMMARY.md`
</output>
